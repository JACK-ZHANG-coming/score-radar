# 2026-09-13

## 学生成绩分析模块 commit 076a28b
- 按用户明确要求提交本地代码及日志:路由骨架(router/index.js + Layout.vue)、五个 analysis/ 占位页、两份记忆文件,共 9 文件 291+/32-,commit 076a28b(message 中文,含修复项说明)。推送仍由用户 GitHub Desktop 手动完成。

## 当前未决 bug(用户反馈,尚未修复)
- 现象:点击其他路由后再点「学生成绩分析」一级标题,分组展开后迅速收起。
- 已读源码:menu.mjs(openedMenus 状态机、watch(items, initMenu)、watch(defaultActive))、sub-menu.mjs(title handleClick→handleSubMenuClick 翻转)、menu-item(activeIndex 计算)。
- /tmp/qa-menu-collapse 用 happy-dom + 项目真实依赖(vue 3.5.42/router 4.6.4/EP 2.14.5)复刻 Layout 菜单三轮模拟未复现最终收起态,但稳定观察到点标题时 @close /analysis(用户点击翻转)与 @open /analysis(导航后 initMenu 重开)的竞争;真实浏览器中该竞争的胜负可能受路由组件懒加载/守卫异步时序影响,与用户实测表现吻合,待进一步定位(el-menu 组件 key=collapse 固定不重建菜单树,疑点在 watch(items)触发时机)。
- 临时环境已清理(node_modules/happy-dom 软链已删)。

## 未决 bug 结案(用户换思路)
- 用户明确「未决 bug 已不用解决,换了个思路」:点击其他路由后再点「学生成绩分析」一级标题展开后迅速收起的 close/open 竞争问题**不做修复**。
- 实际状态:用户已在提交 076a28b 中同步移除 div.sub-menu-title 上的 @click 与 handleGroupClick 函数(git diff 076a28b 为空,库与工作区一致)。现一级标题点击仅走 Element Plus 原生展开/收起;跳转「成绩分析总览」仍由路由层 redirect /analysis/overview 兜底(URL 直达),侧栏点一级标题不自动跳第一子项。router redirect 与 sub-menu 结构未动。
- 遗留结论归档:EP menu 状态机竞争根因分析在 2026-09-13.md 上半段 + /tmp/qa-menu-collapse 三轮模拟;若未来要恢复「点一级标题即跳首子项」,需处理 handleSubMenuClick 翻转与导航后 watch(items,initMenu) 重开的竞争(如仅在收起态时才 push)。
