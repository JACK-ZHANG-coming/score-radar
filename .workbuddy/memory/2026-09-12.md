# 2026-09-12 工作日志

## 项目启动(20:57)
- 拉起前后端(3000/5174)双服务供当日测试使用。

## 第五轮增量:批次列表「班级」派生字段(21:12-21:38,团队 software-paper-batch-class,未 commit)
- 需求:批次列表新增「班级」列,按批号第一个 `-` 前段解析(16-电子表格6_1→16班);多 `-` 取最前;无 `-`/前段空/批号空 → null 前端显示 —。
- 实现(2 文件 6 处):后端 paper-batches.js 新增 deriveClass + attachDerived 下发 class + 导出 14 列(班级@index2 紧跟批号);前端 PaperBatchList.vue COLUMN_DEFS 加列(批号后,不可排序)+ 模板 class 分支(null→—)+ mergeSavedOrder 列序合并(旧 localStorage 存档无 class 键时插入到定义前邻列之后,不追加末尾)。导入模板 13 列不动;sync/预填零改动。
- 工程师自测 4 行 class 全对 + build 通过(过程中两次编辑异常丢失,git diff 全量核对无残留);QA 终验 7/7 PASS(8 例边界注入含 16--x→16班/导出解包 6 断言/四场景 mergeSavedOrder 推演,其沙箱自修 1 处非产品缺陷)。
- 实库 4 行批号均含 `-` 前段数字(5-电子表格1-2_1/5-电子表格4_1/7-电子表格1-2_1/16-电子表格6_1)。历史批号 20260901160700 类(无-)当前不在库,场景由 QA 边界注入覆盖。

## 学生成绩分析模块·路由骨架交付(software-company 团队快速模式)
- 需求:一级路由「学生成绩分析」置于「学生成绩记录」上方,五二级路由(成绩分析总览/不及格管理/优生管理/异常学生画像/进步学生画像),点一级默认跳「成绩分析总览」,占位页仅留结构。
- 实现:router/index.js 新增 analysis 一级(redirect /analysis/overview)+五嵌套子路由;Layout.vue menus 分组渲染(el-sub-menu children)+div.sub-menu-title 挂 handleGroupClick(通用 children[0].path)+折叠态 span 隐藏补丁+面包屑三级 groupTitle;新增 web/src/views/analysis/ 五占位页(el-card+el-empty 纯模板)。
- BugFix 链:@click 挂 el-sub-menu 根 → 二级点击冒泡劫持(QA 实证 double-push race cancelled)→ 移入 #title 内层 div 修复;随后 EP 折叠直接子 span 失配 → .el-menu--collapse .sub-menu-title span 隐藏补丁(构建产物实证命中)。
- QA 两轮:首轮 7P1F、二轮全 PASS(build×3、路由推演 25/25、DOM 冒泡模拟、编译产物断言);零新依赖、零后端改动、git 未动。团队 software-score-analysis 已 TeamDelete 清理。

## 分析占位页布局调整(用户反馈)
- 用户反馈占位页内容未撑满窗口(截图红框)。root cause:占位页用 el-row justify=center + el-col :lg=12 居中限宽,与既有页面(卡片直接放根 div 自动满宽)风格不一致。
- 修复:五个 analysis/*.vue 统一改为根 div > el-card.page-card(与 ScoreList/PaperBatchList/StudentList 同构),去掉 el-row/el-col。build ✓。
