# 2026-09-14

## 不及格管理按参考图重做(用户提读 Desktop/参考界面.png)
- 参考图结构:顶部「考试批次:批次名列表」信息条 +「不及格率」行(每场次列上方百分比)+ 粉色场次号行(序号|1|2|3...)+ 矩阵(序号1-N,列=场次,单元格=不及格学生姓名)+ 右侧独立汇总表(姓名|不及格次数)。
- 页面重写 AnalysisFailures.vue:info-bar(考试批次列表) + board-wrap flex(左 matrix-panel 矩阵 + 右 summary-panel 240px 汇总) + 图例;表头两行(上=不及格率%灰字,下=场次号粉底 #f9a8c4,场次号带 tooltip 显示批次全名);单元格白底黑字姓名,分数小字右下(11px 灰),订正/后续及格用文字标记(绿✓/橙↗)不整格变色,保留图例。
- 汇总面板标题「X班（共N人）」用 summary.classStudentCount;汇总表 rows(name+failCount,次数>=2红tag/1橙tag)。
- 数据全动态:5班=批次2列、49.12%/14.29%、57人、28行;7班 41 行;16班 18 行;0 条=占位文案+空汇总(此前副本库已验证)。
- 双端生效:5174 dev(HMR 自动)+ 3000 静态(需 build,已构建)。vite build ✓。

## 不及格管理表头三行统一(用户再次调整)
- 表头改为三行且完全统一:考试批次(批次全名)/不及格率(%)/序号(场次编号),三行共用 .th-row(12px、font-weight 400、#606266、居中、nowrap+ellipsis、无背景);序号列上两行留空占位,第三行只显示「序号」本身,不再拼接任何批次信息。
- 移除:顶部 info-bar(考试批次列表,避免与表头重复)、场次号粉色底 .th-no/#f9a8c4、tooltip 省略号、.th-rate/.th-rate-label 旧样式、批次列的 tooltip。
- 批次唯一性由后端 SQL DISTINCT 保证,实测三个班级均无重复列(5班2/7班1/16班1)。
- 其他(班级按钮组、矩阵、右侧汇总、图例、空状态)保持不变;vite build ✓。

## 不及格管理表头追加日期与总分(纯前端展示)
- 新增两个格式化函数(纯前端,接口结构未动):formatShortDate(2026-09-02→26.9.2,年取两位+月日去零,非法/空返回'')、batchLabel(批次名+（26.9.2）,日期空则省略括号)、rateLabel(49.12%（总分：20）,totalFull 为空/0/非数字则省略括号部分,避免「（）」)。
- 列 min-width 150→180 容纳更长表头;表头行加 title 属性(悬停看完整文本,th-row 仍 nowrap+ellipsis 保证三行对齐)。
- 验证:5班 5-电子表格1-2_1（26.9.2）/49.12%（总分：20）、5-电子表格4_1（26.9.9）/14.29%（总分：10）;7班、16班同理;空值5例边界(空串/null/0 满分/非法日期/跨年)均无异常括号。vite build ✓。

## 试卷批次列改固定宽度 110px
- 批次列 min-width=180 → width=110(固定,不随内容/容器伸缩);序号列 width=80、右侧汇总表(min-width 100/96)均未动;矩阵表格无 fixed 列、仅 max-height=600 垂直滚动,表头与单元格同列宽天然对齐。
- 取值依据(12-13px 字号实测估算):典型批次号 "5-电子表格1-2_1" 94px+16内边距=110px;最长 "16-电子表格6_1" 104px;单元格姓名4字 68px、分数小字 "100.00 分" 69px → 110px 恰好容纳批次号与单元格内容。带日期的 "…（26.9.2）" 174px 与率行 "49.12%（总分：20）" 129px 会截断,靠 title 提示。
- 防溢出:.stu-cell width:100%+overflow:hidden;.stu-name nowrap+ellipsis+title;分数小字加 title(满分/及格线);表头两行已有 title。
- 副作用提示:全部列固定宽后若总宽小于容器,表格右侧会留白(EP 仅给 min-width 列分配剩余空间);如需撑满需把末列改 min-width(会破坏全固定)。

## 试卷批号列表头改四行排版
- 表头由 3 行变 4 行:批次名 /（时间：26.9.3）/ 66.13%（20）/ 场次编号(编号值不变)。batchLabel 废弃,新增 dateLabel(（时间：x.x.x）,空则返回''),rateLabel 去掉「总分：」改为「%（20）」。
- 新增 hasDateRow computed(任一批次有日期才渲染时间行),序号列同步渲染相同行数占位 → 各列行数一致、横向不错位;日期全空时整行省略不占高度。
- .th-row 由 nowrap+ellipsis 改为 white-space:normal + overflow-wrap/word-break:break-word(长批次名折行不截断),line-height 1.7→1.5 更紧凑;保留 title 悬浮看完整。
- 验证:5班/7班/16班四行输出正确;边界(空日期隐藏行、totalFull=0 只显示百分比、超长批次名+跨年日期)均无空括号、无截断。vite build ✓。

## 姓名单元格改单行紧凑「陈子婕 0 分↗」
- 结构:原 flex column 三行(姓名/分数/状态文案) → 单行 flex row baseline:姓名(13px 600) + gap4 + .stu-meta(11px 灰,含「X 分」+状态箭头,不收缩)。padding 4/8→2/6,line-height 1.3。
- 状态:新增 cellMarkArrow(✓订正/↗后及格)只显示箭头;完整状态文案(后及格 ↗/订正 15 ✓)移入 cellTitle(姓名+分数+满分/及格线+状态)作为 title 悬浮,语义不丢失。补回 .mark-corrected(#529b2e)/.mark-later(#b88230) 箭头配色(重写时曾丢失)。
- 兜底:.stu-name min-width:0+ellipsis(可收缩),.stu-meta flex 0 0 auto(分数与箭头始终完整),容器 overflow hidden → 不换行/不溢出。
- 实测:典型「陈子婕 0 分↗」72px;极端「欧阳文轩 100 分↗」97px 仍在 98px 可用宽内单行容纳。vite build ✓。

## 姓名截断修复:压缩批次列 .cell 内边距(关键坑)
- 根因:Element Plus 单元格内容层 div.cell 默认 padding 0 12px,与列内 padding 叠加后 110px 列宽实际可用仅约 74px →「李可凡 0 分↗」(约81px)触发 .stu-name 的 ellipsis 被截断。
- 修复:批次列加 class-name="batch-col",用 .matrix-panel :deep(.batch-col .cell){padding-left/right:2px} 精准压缩(表头与数据单元格同时生效,上下对齐一致);.stu-cell padding 2px 6px→2px 2px。列宽仍固定 110px 未变,序号列与右侧汇总表不受影响(:deep 限定在批次列 class 内)。
- 可用宽度 74px→102px;全量 95 条记录校验 0 条超限,最宽「陈可凡 10.25 分↗」101px,「李可凡 0 分↗」77px → 省略号不再出现。vite build ✓。
- 经验:调 el-table 单元格内边距不能只改自己的 div 或 td(cell-style 只作用于 td),须覆盖内层 .cell 的 padding。

## 右侧「班级人数」模块补全(全部班级 + 零不及格人数)
- 后端 analysis.js 新增 computeClassStats(clazz):基于该班全部成绩(不去重过滤)算 studentCount(DISTINCT name)/failStudentCount(已配置批次 total<passLine 的姓名去重)/zeroFailCount=两者之差;/classes 接口扩展返回 stats 数组(逐班统计,无不及格班级照常返回),classes 仍为字符串数组(按钮组无需改动)。
- 前端:新增 classStats ref + totalZeroFail computed;右侧面板上方新增「班级人数统计」表(班级/总人数/零不及格人数,show-summary 合计行,零不及格列用绿色 tag + 自定义合计 footer),下方保留「X班（共N人）」不及格名单表;面板宽度 240→270px。
- 验证(真实库):5班 57/30/27、7班 62/41/21、16班 52/18/34,合计 171 人/零不及格 82,恒等 总=有不及格+零不及格 ✓。副本边界(16班全及格 + 新增 9班1人及格):4 个班全部列出,零不及格班级 zeroFailCount=总人数 ✓,9班 failures 返回 batches=1/rows=0 不报错。副本已删。vite build ✓。

## 删除班级人数统计表 + 零不及格学生并入统一名单
- 后端 analysis.js:rows 去掉 `if(!failRecords.length) return;` 过滤 → 该班全部学生进 rows(零不及格者 failCount=0/cells={}/failScores=[]);summary 增加 zeroFailCount;failStudentCount 改为 rows.filter(failCount>0).length(语义不变);排序保持 failCount desc + name asc(0 次自然排最后)。
- 前端:删除「班级人数统计」表(及 classStats/totalZeroFail/class-stats-table 样式),面板宽度 270→240;右侧仅保留「X班（共N人）」+ 全量名单表;tag 配色加 0 次 info 灰(failCountTagType);空状态条件改为 !classes.length || !batches.length(无人不及格也正常展示全量名单),hasFailData 仅用于图例显隐。
- 坑:Edit 替换造成 rankRows 重复声明(两处同名 computed)→ vite build 报 "Identifier 'rankRows' has already been declared",删除重复处后通过。教训:删改 script 变量时改完先 build 或 grep 确认唯一性。
- 验证:5班 57 人(30 不及格/27 零)、7班 62(41/21)、16班 52(18/34);次数降序 ✓、同次数按姓名 ✓、0 次排末位 ✓;矩阵 students 仍只含不及格学生(28/8、41、18)未受影响。

## 成绩导入改多文件(每文件独立批号)
- 后端 scores.js:抽取 importOneFile(file,batchNo,overwrite)(解析/表头校验/冲突预扫描/事务写入,结果 {ok,inserted,updated,errors,message,conflict,conflictCount});模块级预编译 importInsert/importUpdate/findByBatchName。原 /import 改为复用该函数(行为不变,仍 409 duplicate),新增 POST /api/scores/import-multi(upload.array('files',20) + batchNos JSON 数组 + overwrite),逐文件独立事务,单文件失败仅该文件失败,返回 {summary:{totalFiles,successFiles,failedFiles,inserted,updated,skipped}, details:[{fileName,batchNo,ok,inserted,updated,skipped,message,conflict,errors(前10条)]}。
- 前端 ScoreList.vue:导入弹窗 520→780px,el-upload multiple + :show-file-list=false,自定义文件列表(序号/文件名/大小/可编辑批号/移除)+「重复时覆盖」开关;校验:类型 .xlsx/.xls、单文件≤10MB、最多20个、空文件、重复同名同大小、批号为空、同批次批号重复(validateImportFiles 实时提示);新增结果汇总弹窗(成功/失败文件数 + 各文件新增/更新/跳过 + 失败原因)。api/scores.js 新增 importScoresMulti(timeout 120s)。
- 数据流转:files 按列表顺序逐个 append('files') + batchNos=JSON.stringify(顺序一致) + overwrite → 后端按下标取 batchNos[idx](空则回退文件名去扩展名)。
- 验证(副本库,真实库未污染):3 文件(2 正常+1 损坏)→ 成功2/失败1,损坏文件不影响其他;批号空→回退文件名;重复冲突未覆盖→该文件失败并提示;勾选覆盖→更新2条。vite build ✓。
