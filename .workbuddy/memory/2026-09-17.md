# 2026-09-17 score-radar 工作日志

## 交卷时间宽容解析（快速模式交付，未 commit）
- 需求：导入/录入交卷时间非 YYYY-MM-DD HH:mm 自动归一化（示例 `2026/9/16 星期三 11:34:37` → `2026-09-16 11:34`）。
- 改动仅一处：server/src/utils/excel.js normalizeTime（唯一时间归一化入口，覆盖 scores.js:167 手动录入 / :387 Excel 导入 / seed.js:91 三条路径）。
- 新正则：`^(\d{4})[-/.年](\d{1,2})[-/.月](\d{1,2})日?(?:\s*(?:星期|周|礼拜)[一二三四五六日天]?\s*)?(?:[\sT]+(\d{1,2}):(\d{1,2})(?::(\d{1,2}))?)?$`；月日越界校验含闰年，越界/不可解析原样返回（宁保留原值不伪造日期）；数字序列号路径与既有无中缀路径零回归。
- QA 两轮：单测 34 条 + 集成 5 项（HTTP 入口含真实 xlsx 导入）全 PASS；P3 已知行为：纯 "2026" 字符串会补成 2026-01-01 00:00（接受）。主理人独立抽验 10/10 复核一致。
- **教训：代理拉起的临时后端进程会随代理退出被回收**——QA 第二轮重启验证止杀旧实例属预期，但其临时实例在代理退出后消失，3000 端口一度 000。会后以主会话 run_in_background 重新拉起（Task XqmjGM）并复测 health+login 200。后续会话凡涉及服务重启，统一由主会话后台任务承载，勿借代理子进程。

## 服务状态
- 后端 127.0.0.1:3000（Task XqmjGM），前端 Vite 5174（Task a8EXCl）均存活。

## 第六轮增量:列序+一键总满分+班级筛选(14:52-16:40,团队 software-paper-batch-round6,未 commit)
- 三项:①COLUMN_DEFS 总满分/创建时间提前到批次名称后+导出同步+LEGACY_DEFAULT_ORDER 存档迁移(旧默认序档自动重排,自定义序保留);②一键设置总满分:接口11 preview(逐批次 maxTotal/willChange)+接口12 apply(服务端重算+单事务)+魔棒按钮弹窗表格勾选(willChange 默认选/其余禁选)+配套放宽 validateScoreByConfig(分项满分0=该科不限制,总分上限保留);③班级筛选:接口13 class-options(批次派生∪scores.class 去重数字升序)+列表/导出 class 过滤(派生列 SQL 外过滤后分页,排序用过滤后 id 集二次查询)。5 文件 +325 行。
- 后端遗留:3000 端口陈旧进程不热载(mtime 变更不触发重启?!)→主会话 kill+run_in_background 重启(qGYMm9)后新路由 401 探活确认。**再证 09-17 教训:代理子进程会随代理退出回收,服务由主会话承载。**
- excel.js normalizeTime(09-17 上午交付)与第六轮代码同在工作区,提交时一并入库。
- QA 终验 14/14 PASS;**事故披露**:QA harness require 钩子漏了 utils/paperBatch.js,ensurePaperBatch 兜底向实库写 3 行 QA-占位(id45-47),已由 QA 用产品接口删除;主理人 sqlite3+live API 独立复核:4 行原值 10/20/10/20、零残留、preview willChange 全 false、git 无波动。教训:harness 打桩必须覆盖 require 图所有 db 落点(routes+utils)。
- 实测口径备忘:四批次 total_full 恰都等于各自 MAX(total),故当前一键总满分首点提示"无需调整";演示弹窗需先手动改小某批次总满分。导出新表头(14列)再导入实测兼容(按 header 名取列)。

## 第六轮口径修订:一键设置总满分仅补 0 值批次(22:07,主理人直改,未 commit)
- 用户修订:总满分>0(已配置)的批次不参与批量设置,只批量补「当前为 0」的。
- 改动 5 处:preview willChange=!(currentTotal>0)&&maxTotal!=null;apply 增加 currentTotal>0 跳过分支(计入 skipped「总满分已配置(>0),不覆盖」);前端提示/弹窗警示/底注/表格列头(是否变化→待设置)文案同步。
- 实测:实库 5 批次(用户下午新增 17-Access综2_1,curr=max=10)全部 willChange=false ✅;build 3.02s 通过。node --watch 热载生效。
