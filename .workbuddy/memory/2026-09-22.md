# 2026-09-22 score-radar 工作日志

## 不及格管理页「不显示已订正」开关（快速模式交付，未 commit）
- 需求：标题行新增开关「不显示已订正」；开启时矩阵即时隐藏已订正合格（status='corrected'）学生，仅显示尚未订正或订正后仍不及格；关闭恢复全部；即时生效无需刷新。
- 改动单文件 `web/src/views/analysis/AnalysisFailures.vue`（+40/-3）：el-switch `hideCorrected`（默认 false），置于 .filter-group 与 .el-radio-group 之间；核心 `displayBatches` computed——开启时 map 浅拷贝批次并 `filter(stu => stu.status !== 'corrected')`，关闭直返原数组；矩阵 v-for 与 matrixRows 行数改读 displayBatches。
- **口径铁律落地**：只隐藏 corrected；laterPass（后续已及格但未订正）保留显示；表头 failCount/failRate 保持后端原始口径（视图过滤非数据修改），右侧名单 rankRows/图例/空状态不动。
- QA 两轮：真实 API 基线（4 班 13 批次）+ T1-T5 断言 + vite build，7/7 PASS 路由 NoOne。注意：**当前生产库 corrected=0（无订正分记录），开关主路径只有合成 fixture 覆盖**——端到端可见效果待有真实订正数据后复核；5 班 laterPass=21 真实路径已验证。
- 服务状态：后端 3000（efoWCw）+ 前端 5174（CyYdQ8）后台任务承载中。

## 环境教训（会话操作层面）
- 本地服务探活：/health 在 3000 返回 SPA 兜底 HTML（无独立 JSON 健康路由），以登录/API 可达为准；curl [::1]:5174 需 `-6 --noproxy '*'` + URL 引号（zsh 会把 [] 当 glob）。

## 缺陷修复轮：状态互斥单选 → marks 多状态聚合（未 commit）
- 用户报告：开关点了没反应；后续纠正口径：**laterPass≠已闭环**，真正缺陷是后端把多种命中状态压缩成 status 三选一互斥（corrected 优先 > laterPass > fail），corrected 与 laterPass 并存时只显示一种。真实库 336 条 correction_score 全 NULL（corrected 实态从不出现）是「开关无反应」帮凶：当时把陈子婕(laterPass, 0分)误判为「已闭环」隐藏了 laterPass，经用户纠正回滚。
- 修复（主理人亲自执行——**软件工程师代理两轮派发均 killed 零产出，多智能体协作在本环境对长编码任务不稳定，短任务可用**）：
  1. `server/src/routes/analysis.js` /failures：cells 新增 `marks` 数组（corrected 与 laterPass 独立判定可并存，双未命中=['fail']）；计数口径微调——corrected 计数优先，else if laterPass（不计双命中），rows/students.push 同步透传 marks；status 保留为向后兼容主导态。
  2. `web/src/views/analysis/AnalysisFailures.vue`：cellMarkText/Arrow/Class 三函数 → cellMarks(数组)/markText/markArrow/markClass；模板箭头区改 v-for 多标签渲染（每 mark 独立配色/title）；cellTitle 悬浮信息列全量状态；**开关过滤回滚为 status !== 'corrected'**（精确隐藏，laterPass 保留）。
- 验证：node --check + vite build ✓；后端重启后真实 API（陈子婕 marks=['laterPass'] 等 3 项 PASS）+ 合成 5 用例（双状态并存聚合 PASS）+ 开关复算 PASS + 前端 cellMarks 新旧数据双兼容 PASS。
- 服务：后端换新实例（Task y9KvRK，旧 76663 已停）；前端 Vite HMR 热替换。

## 缺陷修复轮 2：订正分只读总分列，单科订正分从未生效（未 commit）
- 用户场景：陈子婕电子表格订正分 20（correction_spreadsheet=20.0），点开关仍显示。根因三层链：数据层 56 条单科订正分躺在 correction_choice/spreadsheet/access/python/composite 列；SQL 层 getScoresOfClass 只查 correction_total AS correctionScore；构造层 `subjectKey==='total'` 才带订正分、单科一律 null → 单科口径 corrected 永远判不出 → 开关（正确口径 status!=='corrected'）无效。
- 修复（analysis.js 三处 + 注释）：SUBJECTS 增 per-subject `corrKey` 映射六列；SQL 查全六列（correctionTotal/Choice/Spreadsheet/Access/Python/Composite）；records 构造按口径取 `r[subj.corrKey]`。前端零改动（marks 链自动吃新值）。
- 端到端验证（5班电子表格口径）：陈子婕 status=corrected·marks=[corrected,laterPass]·correctionScore=20（订正 ✓+后及格 ↗ 双标记并存展示）；批内 1 corrected + 6 fail（订正 0 或未订正正确保留）+ 21 laterPass；总成绩口径对照无回归（correction_total 空 → 保持 laterPass）。
- 服务：后端新实例 Task scAEF6。**认知修正：此页「订正分」语义 = 当前判定口径科目的订正分（各科独立录入），不是只有总分校正；correction_score 列为旧遗留（页面菜单里的订正分=correction_score），analysis 已绕开它直查六科订正列。**

## 缺陷修复轮 3：右侧名单不及格次数与矩阵口径脱节（未 commit）
- 现象：开启开关后矩阵隐藏 corrected 学生，但右侧名单「不及格次数」仍为后端原始 failCount（陈子婕 2，其中 1 次已订正），排序也不变——与矩阵口径矛盾。
- 修复（AnalysisFailures.vue 仅 rankRows 一处）：开关开启时，名单行派生副本 failCount → max(0, failCount - correctedCount)（有效次数=剔除已订正通过的那几次），并按有效次数降序+姓名升序重排；关闭回原始 rows。与矩阵 displayBatches 同构（派生不污染原文数）。
- 验证（真实电子表格口径数据复算）：陈子婕 2→1 + 排序降序 PASS + 负数兜底 PASS + 矩阵/右侧同减唯一对象一致 PASS；vite build ✓。

## 学生成绩记录页·总成绩自动累计（快速模式交付，未 commit）
- 需求：编辑/新增弹窗中改五科任一项，总成绩自动重算为五科之和。
- 改动单文件 `web/src/views/ScoreList.vue`（+27/-2）：`sumOfSubjects` computed（toNum 兜底 null/undefined→0 + Math.round 2 位防浮点长尾）+ `watch` 始终跟随口径（与 PaperBatchList 演进终态一致）；**回填保护**——openDialog 置 restoring=true 拦截 Object.assign 触发的这一次联动，`Object.assign(..., { total: row?.total ?? 0 })` 显式保住历史人工 total，nextTick 复位后恢复联动。用户手改 total 后再改任一科，total 仍跟随（「自动累计」语义优先）。
- 工程师交付漏 import nextTick（主理人复核补上）；vite build 3.18s 通过。
- QA 快照复刻逻辑 9/9 PASS（S1 联动求和/S2 回填不改写历史 total/S3 联动恢复/S4 null 无 NaN/S5 10.1+20.2=30.3/S6 新增态），主理人亲测复跑确认。测试脚本 /tmp/qa_total/qa_total_test.js。
- 团队 software-score-total（engineer+qa 两成员）已全部 shutdown。

## 优生管理页·比例下拉新增 100%（快速模式，未 commit）
- 需求：比例下拉 80/85/90 → 增加 100%。**后端零改动**：`/top-students` 校验为 `ratioRaw > 0 && ratioRaw <= 100`，100 本就合法；优秀线 = 满分×100/100 = 满分，只有满分卷达优。
- 改动仅 2 行：`web/src/views/analysis/AnalysisTopStudents.vue:166` 加 `{ value: 100, label: '100%' },`（90 之后、升序）；`web/src/api/analysis.js:12` 注释同步为「80/85/90/100，默认 80」（避免过期注释误导）。默认值 `ratio = ref(80)` **未变**。
- QA 34/34 PASS、路由 NoOne。关键实证：16 班批次「16-电子表格6_1」**总成绩满分仅 10 分**，100% 档优秀线=10，真实有 **4 人满分达优**（倪子熙/宋雨泽/程思敏/陈展）——勿想当然认为 100% 必然空名单。合成边界：10.0 计入、9.99 不计入（≥ 判定成立）。
- **最易踩的坑已排除**：满分 0 的未配置批次在 100% 档下 `configured=false`、优秀线 0，0 分与 5 分学生**均未被误判达优**。
- 回归：80/85/90 三档响应与 HEAD 基线版逐字节一致（另起 3113 端口跑 HEAD 版对跑）；不及格页 RATIO_OPTIONS 仍 60/70/80 未被误改。
- **工作区混杂提醒**：本轮开工时 HEAD 已推进到 `830310e`，工作区 8 项改动中只有 2 项属本次（AnalysisTopStudents.vue + api/analysis.js），其余（ScoreList.vue、AnalysisFailures.vue、analysis.js /failures 订正分、deploy/DEPLOY.md）为既有/并行任务改动，本轮一概未碰。
- 团队 software-top-ratio100 已解散。

## 订正总成绩自动累计（2026-09-22 下午，未 commit）
- 需求：上轮做「总成绩自动累计」时只覆盖五科原始分联动 total，订正分各科联动 correction_total 缺失——用户改订正分时总订正不更新。
- `web/src/views/ScoreList.vue`（仍在单文件上，diff +68/-2 含上轮未 commit 部分）：
  - 新增 `corrNum`（null/undefined/''/非法数字→null）、`CORR_FIELDS` 五科映射、`sumOfCorrections` computed（订正科取订正分、未订正科取原始分；五科订正全 null→返回 null 不联动，复用 analysis「订正了哪科哪科生效」口径）、`watch(sumOfCorrections)` 复用同一 `restoring` 标志写 `form.correction_total`。
  - `openDialog` 回填 `correction_total` 同 total 保护：历史人工录入值（≠各自合计）打开弹窗不被静默改写；v=null（用户清空全部订正）→ 总订正同步归 null。
- 口径注意：未订正科取原始分是因为已订正科目在「订正后」语义下总分应以订正值重算——与 analysis 订正判定口径一致；五科全未订正保持 null 不联动，避免给从未订正的学生凭空算出总订正。
- QA：/tmp/qa_corr/qa_corr_test.js（@vue/reactivity 单元复刻）10/10 PASS；vite build 3.19s 通过。
