# 2026-09-15

## 部署实录·cjgl.zhangqiang.hk.cn 上线完成（Agent 模式执行，未 commit）
- **服务器真实约定与 DEPLOY.md 原假设不同**（教训：部署前先摸清服务器惯例再写文档）：无 /www/wwwroot，实际代码目录约定是 **/var/www/<站点名>**（ai-manage/aitools 同款）；nginx 走 **sites-available + sites-enabled 软链**（conf.d 空目录）；certbot 已管 5 张证书（authenticator=nginx 的 ai-manage + webroot 的我们）。Node v22.23.2、build-essential/python3 齐、pm2 7.0.4 新装、3000 空闲。
- 部署路径：DNS（用户已加，A 记录即时生效）→ rsync 到 /var/www/cjgl → 数据用 sqlite3 .backup 在线备份迁移（1252 学生/227 成绩/4 批次）→ 服务器 npm install（better-sqlite3 编译一次通过）→ JWT_SECRET openssl rand -hex 32 仅存服务器 ecosystem.config.js → pm2 start/Save/startup → nginx 两阶段（80 only → certbot webroot 签证书 → 443+http2）。
- ACME webroot 独立目录 /var/www/cjgl-webroot（避免与代码目录 /var/www/cjgl 同名混淆）。证书有效期至 **2026-12-14**，certbot renew --dry-run 全绿（含其余 5 张证书）。备份 crontab 凌晨 3 点已装（保留原 stargate 任务）。
- 验证实录：HTTPS 200 / SSL 验证 0 / **协议 h2 生效**；深链 /analysis/overview 回退 SPA 页面；API 404 语义保留；admin/admin123 登录成功且 analysis/classes 返回真实班级数据（5/7/16班）；ai-manage/aitools 现有站点 200 不受影响。
- **遗留坑（已写进 DEPLOY.md 已知注意点）**：增量 rsync 本地→服务器方向会把仓库占位 JWT_SECRET **覆盖掉服务器真实密钥** —— 后续更新必须 --exclude 'deploy/pm2/ecosystem.config.js'（或同步后服务器重新替换+pm2 restart）。
- SSH 多行脚本的引号转义在 RemoteExec 里易碎（awk/sed 双引号被外层撕），改「本地生成文件 + scp」模式一次过；BSD sed -i '' 空后缀歧义改用 perl -pi -e。certbot dry-run 6 张证书耗时约 3 分钟，SSH 命令要给足超时或转后台。
- 本机默认 DNS 对新域名解析滞后（公共 DNS 已生效），curl 验证用 --resolve cjgl.zhangqiang.hk.cn:443:43.134.106.173 绕过；--noproxy '*' 照旧必加。
- 待用户行动：浏览器实访 https://cjgl.zhangqiang.hk.cn 并**首登改密**；git 提交由用户测试后决定。

## 部署方案设计·cjgl.zhangqiang.hk.cn（软件团队快速模式，未 commit）
- 需求：score-radar 发布到用户服务器 43.134.106.173（Ubuntu 24.04 + nginx + LE 证书既有体系），域名 cjgl.zhangqiang.hk.cn。形态：浏览器 → nginx(443) → 127.0.0.1:3000 Express 单进程（托管 web/dist + /api + sqlite）。与 ai-manage/aitools 站点同模式，conf.d 独立文件互不影响。
- 发现并修复阻断项：**history 路由深链 404**——server/src/index.js 原有 404 JSON 兜底会把 /analysis/overview 刷新请求返回 JSON 而非页面。已在 /api 路由后、404 兜底前加 SPA 回退中间件（非 /api 的 GET + fs.existsSync 守卫 → sendFile index.html）。QA 实测 13/13（深链/HEAD/查询串/路径穿越/静态优先级/API 404 语义全保）。
- 新增 deploy/ 三件套：nginx/cjgl.zhangqiang.hk.cn.conf（80: ^~ ACME webroot /var/www/cjgl + 301；443: LE 标准证书路径 + 反代 3000 + client_max_body_size 10m 对齐 multer）、pm2/ecosystem.config.js（score-radar，env_production JWT_SECRET 占位待换）、DEPLOY.md（DNS→构建→rsync→服务器 npm install→pm2→nginx 两阶段→验证清单→运维，全命令可复制）。
- 目录约定：代码 /www/wwwroot/cjgl，ACME webroot /var/www/cjgl，两者分离；rsync 排除 node_modules（**better-sqlite3 平台原生二进制，mac 产物不可上 Linux，必须服务器 npm install**，需 build-essential+python3）/server/data（--delete 不删接收端被排除路径，线上库不受 rsync 影响，QA 已推演）。
- **P0 教训：nginx `http2 on;` 独立指令是 1.25.1 引入的，Ubuntu 24.04 默认源 nginx 是 1.24.0——`nginx -t` 会报 unknown directive**。修复：行内 `listen 443 ssl http2;`（1.9.5+ 全兼容），头部注释写明两种写法二选一。写 nginx 配置必须对齐目标环境真实版本，勿用最新版语法。
- P1 教训：crontab 备份的 mkdir 目录必须排在 crontab -e 之前，且命令行兜底 `mkdir -p ... && sqlite3 .backup`；cron 中 % 必须 \% 转义（$(date +\%u) 每周循环覆盖保留 7 天）。
- WAL 迁移数据用 `sqlite3 .db ".backup /tmp/x.db"` 在线备份，不直接拷 .db（可能丢未 checkpoint 写入）。
- 二轮 QA 全绿判定可部署。1.11.0 vs 1.9.5 注释口径小误差（保守方向）未修。favicon 缺失为已知项（web/public 放了重建即可）。
- 待用户操作：DNS 加 A 记录 cjgl → 43.134.106.173 → 按 DEPLOY.md 执行；JWT_SECRET 替换（openssl rand -hex 32）；admin/admin123 首登改密。所有改动未 commit（遵守先测后提纪律）。

## 批次管理「一键修改合格占比」
- 后端 paper-batches.js 新增 POST /api/paper-batches/batch-pass-ratio(body:{passRatio,name,batchNo}):筛选条件与列表接口完全一致(name/batchNo 模糊);db.transaction 包裹整批,单条异常 catch 记明细并继续(部分成功语义);返回 {summary:{total,updated,skipped,failed}, details:{skipped[],failed[]}};跳过规则 notEditableReason:批号为空 / archived=1|true / status 命中 LOCKED_STATUS(archived|finished|ended|locked|disabled|已归档|已结束|已锁定) —— 当前表无状态字段,规则已预留。
- 前端 PaperBatchList.vue:工具栏「自动更新试卷批号」后加 type=warning + EditPen 按钮;流程 ElMessageBox.prompt(默认60,inputValidator 校验 0~100 数字允许小数) → confirm 二次确认(提示影响 total 条) → updatePassRatioBatch({passRatio,...query}) → 成功/失败提示(失败弹明细前10条) → fetchList 刷新;ratioLoading 控制 loading。api 新增 updatePassRatioBatch。
- 验证(副本库,真实库未改):全量改75→成功4/跳过1(归档 ARCH-1)/失败0;筛选 batchNo='5-'→成功2;非法值 120/-5/abc/''/null 均 400 并给对应提示;无匹配筛选→total 0 提示;小数 66.5 正常。vite build ✓。

## 事故:后端服务再次被回收(第二次踩同一个坑)
- 现象:用户访问接口 500。排查 curl 3000 返回 HTTP 000(连接被拒)→ 服务不在运行;Vite dev 代理在后端不可达时对外返回 500,故表现为「接口 500」。
- 原因:清理副本后我用 `node src/index.js &`(子 shell 后台)启动服务,bash 任务结束即被回收。这是本机已知坑,已第二次违反。
- 铁律(务必遵守):本机启动任何长驻服务**必须**用 Bash 的 run_in_background=true,禁止 `(cmd &)` 或 `cmd &`;停止服务用 TaskStop(task_id),禁止宽泛 pkill。
- 恢复:已用 run_in_background 重启(task Yel7W6),验证 /api/health 200、5174 代理 200、批次列表/成绩列表/分析班级均 200、批量占比接口 400/200 正常,无真实 500。
